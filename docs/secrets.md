# Secrets Management

- Store credentials in GitHub Actions secrets (Settings → Secrets and variables).
- Keep local secrets in `.env` (do not commit); use `.env.example` as a template.
- Prefer service-specific tokens with least privilege.
